# データーベースを作成する
# from models.database import init_db
# init_db()

# データーベースにデータを挿入
# from models.database import db_session
# from models.models import Message

# msg1 = Message('Pythonのsum関数について', 'sum関数は引数で渡されたオブジェクトの合計を返してくれます')
# msg2 = Message('掲示板を作った件', '作ってしまった。。。')
# msg3 = Message('よく作ったなこんなもん', '作りたくなかった。。。')

# db_session.add(msg1)
# db_session.add(msg2)
# db_session.add(msg3)
# db_session.commit()
